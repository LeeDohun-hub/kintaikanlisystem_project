________________________________________
📌 로그인 화면 / 직원 마스터 입력(등록) 가이드

이 프로젝트는 **정의서의 테이블(`employee`, `work_time`, `batch_import_history`)** 를 기준으로 구현되어 있으며,
웹 로그인/권한을 위해 최소 확장 테이블 **`employee_account`** 를 추가로 사용합니다.

1) 화면(프론트) 동작 흐름
•	`/login` : 직원 코드(ID) + 비밀번호로 로그인
•	`/register` : **직원(スタッフ) 계정만** 등록(직원 마스터 + 계정 생성)
•	로그인 후
  - 직원(EMPLOYEE): 근무 입력(`/work-input`), 근무 이력(`/work-history`)
  - 관리자(ADMIN): 대시보드(`/dashboard`), 근무 조회(`/work-view`), 통계(`/statistics`)

2) 직원 마스터(정의서: employee) 입력은 어디서?
•	현재 구현에서는 “직원 마스터 등록(= employee 레코드 생성)”을 **회원가입 화면(`/register`)** 에서 수행합니다.
  - 가입 시 생성: `employee` + `employee_account`
  - 기본값:
    - `employee.active_flag` = 1
    - `employee.hourly_cost` = 미입력 시 0
    - `employee.created_at/updated_at` = 서버/DB 기본값으로 자동

3) DB 테이블 요약 (현재 구현)
•	`employee` (정의서)
  - PK: `employee_id`
  - 주요 컬럼: `employee_code`, `employee_name`, `department`, `hourly_cost`, `active_flag`, `created_at`, `updated_at`
•	`employee_account` (확장: 로그인/권한)
  - PK/FK: `employee_id` → `employee.employee_id`
  - 주요 컬럼: `password_hash`(BCrypt), `role`(ADMIN | EMPLOYEE)
•	`work_time` (정의서)
  - PK: `work_id`, FK: `employee_id` → `employee.employee_id`
  - 주요 컬럼: `work_date`, `start_time`, `end_time`, `break_minutes`, `work_minutes`, `created_at`

4) API 엔드포인트(요약)
•	인증
  - `POST /api/auth/register`
  - `POST /api/auth/login`
  - `POST /api/auth/logout`
  - `GET  /api/auth/me`
•	근무 입력/조회(정의서: work_time)
  - `POST /api/worktime`
  - `GET  /api/worktime?month=YYYY-MM`
•	관리자 전용
  - `GET  /api/employees` (직원 마스터 목록)
  - `GET  /api/statistics/monthly?month=YYYY-MM`

5) 요청 예시(JSON)
•	회원가입(직원 마스터 + 계정 생성)
  - `POST /api/auth/register`
  - Body:
    {
      "employeeCode": "EMP003",
      "name": "홍길동",
      "department": "개발팀",
      "hourlyCost": 3000.00,
      "password": "pass123",
      "confirmPassword": "pass123"
    }

•	로그인
  - `POST /api/auth/login`
  - Body:
    {
      "employeeCode": "EMP003",
      "password": "pass123"
    }

•	근무 입력(실근무 분은 서버에서 계산하여 저장)
  - `POST /api/worktime`
  - Body:
    {
      "workDate": "2026-04-07",
      "startTime": "09:00",
      "endTime": "18:00",
      "breakMinutes": 60
    }

6) 주의사항(자주 막히는 포인트)
•	DB에 예전 스키마(`attendance` 등)가 남아 있으면 FK 충돌이 날 수 있습니다.
  - 개발 환경에서는 `kintai_db`를 초기화한 뒤 `sql/schema.sql`을 적용하거나,
    백엔드 기동 시 자동 실행되는 `backend/src/main/resources/db/init-schema.sql`을 사용하세요.
•	프론트는 Vite 프록시(`/api` → `http://localhost:8080`)를 사용하므로
  - 프론트: `npm run dev` (5173)
  - 백엔드: `gradlew.bat bootRun` (8080)
  두 서버가 동시에 떠 있어야 합니다.

